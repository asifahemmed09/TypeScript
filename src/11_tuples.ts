const mix: [string, number] = ["a", 1]

const userEntry: [string, number] = ["John Doe", 24]


type ResponseRow = [status: number, message?: string]

const r: ResponseRow = [300]

const coordinates: readonly [number, number] = [0,0]
